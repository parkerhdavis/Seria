# Getting Started with Seria

This guide will help you install Seria and get started with your first CSV file.

## Installation

### Download Seria

Visit the [Releases page](https://github.com/yourproject/seria/releases) to download the latest version for your operating system.

### Linux Installation

Seria is available in three formats for Linux:

#### AppImage (Recommended for most users)

The AppImage is a universal binary that works on most Linux distributions without installation.

**Steps:**
1. Download `Seria-*.AppImage` from the releases page
2. Make it executable:
   ```bash
   chmod +x Seria-*.AppImage
   ```
3. Run it:
   ```bash
   ./Seria-*.AppImage
   ```

**Optional: Install to system**
```bash
# Move to /opt (or any location you prefer)
sudo mkdir -p /opt/seria
sudo mv Seria-*.AppImage /opt/seria/seria

# Create a symlink for easy command-line access
sudo ln -s /opt/seria/seria /usr/local/bin/seria
```

**Desktop Integration:**
See `resources/README.md` for instructions on:
- Adding Seria to your application menu
- Setting it as the default app for CSV files
- Installing a custom icon

#### Debian/Ubuntu (.deb package)

For Debian-based distributions (Ubuntu, Linux Mint, Pop!_OS, etc.):

```bash
# Download the .deb file
wget https://github.com/yourproject/seria/releases/download/v0.1.0/seria_*_amd64.deb

# Install
sudo dpkg -i seria_*_amd64.deb

# Install dependencies if needed
sudo apt-get install -f

# Launch
seria
```

#### Fedora/RHEL/openSUSE (.rpm package)

For RPM-based distributions:

```bash
# Download the .rpm file
wget https://github.com/yourproject/seria/releases/download/v0.1.0/seria-*.rpm

# Fedora/RHEL
sudo dnf install ./seria-*.rpm

# OR openSUSE
sudo zypper install ./seria-*.rpm

# Launch
seria
```

### Windows Installation

Seria provides two installer formats for Windows:

#### MSI Installer (Recommended)

1. Download `Seria_*_x64_en-US.msi`
2. Double-click the installer
3. Follow the installation wizard
4. Launch from Start Menu or Desktop shortcut

Default installation location: `C:\Program Files\Seria\`

#### NSIS Installer

1. Download `Seria_*_x64-setup.exe`
2. Double-click the installer
3. Choose installation options
4. Launch from Start Menu or Desktop shortcut

**Note:** Windows may show a SmartScreen warning for unsigned applications. Click "More info" → "Run anyway" to proceed.

### macOS Installation

1. Download `Seria_*.dmg`
2. Double-click the DMG file to mount it
3. Drag `Seria.app` to your Applications folder
4. Launch from Applications or Spotlight

**First Launch:**
macOS Gatekeeper may block unsigned apps. To open Seria:
1. Right-click `Seria.app` in Applications
2. Select "Open"
3. Click "Open" in the security dialog

This only needs to be done once. After that, you can launch Seria normally.

## System Requirements

### Minimum Requirements

**All Platforms:**
- 2 GB RAM
- 100 MB free disk space
- 1280x720 screen resolution

**Linux:**
- WebKitGTK 4.1 or later
- GTK 3 or later
- Modern Linux distribution (Ubuntu 20.04+, Fedora 35+, etc.)

**Windows:**
- Windows 10 version 1809 or later
- WebView2 (installed automatically)

**macOS:**
- macOS 10.15 (Catalina) or later

### Recommended Requirements

- 4 GB RAM (for large CSV files with 10,000+ rows)
- 1920x1080 or higher screen resolution

## First Launch

When you first launch Seria, you'll see:

- **Clean interface** - No file loaded yet
- **Toolbar** - Open, Save, and other file operations
- **Empty grid** - Ready to open your first CSV
- **Print preview panel** - Hidden by default (toggle with toolbar button)

## Quick Tour

### Main Interface

**Header (Top):**
- **Open** - Open a CSV file
- **Save** - Save changes to the current file
- **Save As** - Save to a new location
- **File Info** - Current file name and status
- **Theme Toggle** - Switch between light and dark mode
- **Settings** - Application preferences

**Editor (Center):**
- **CSV Grid** - Spreadsheet-like editing area
- **Column Headers** - Click to sort, filter icon to filter
- **Row Numbers** - Click and drag to reorder rows
- **Summary Row** - Column statistics at bottom

**Sidebars (Optional):**
- **Left Sidebar** - File tree (toggle with `Ctrl+.`)
- **Right/Bottom Panel** - Print preview (toggle with toolbar button)

### Essential Operations

**Opening a file:**
- Click **Open** button
- Or press `Ctrl+O`
- Or drag and drop a CSV file onto the window
- Or use the file tree sidebar

**Editing a cell:**
- Double-click the cell
- Or click once to select, then press `F2` or `Enter`
- Type your changes
- Press `Enter` to save and move to next row

**Saving:**
- Click **Save** button
- Or press `Ctrl+S`
- File is saved immediately

**Undo/Redo:**
- Undo: `Ctrl+Z`
- Redo: `Ctrl+Shift+Z` or `Ctrl+Y`

## Next Steps

Now that Seria is installed, continue with:

1. **[Your First CSV](../01%20-%20Cell%20View/01.01%20-%20Loading%20and%20Saving.md)** - Learn basic editing workflow
2. **[CSV Editing Guide](../01%20-%20Cell%20View/01.04%20-%20Cell%20Actions.md)** - Comprehensive feature reference
3. **[Print System Guide](../02%20-%20Print%20View/02.00%20-%20The%20Print%20View.md)** - Preview your data in professional formats
4. **[Keyboard Shortcuts](../03%20-%20Customization/03.02%20-%20Keyboard%20Shortcuts.md)** - Work faster with shortcuts

## Troubleshooting Installation

### Linux: "Permission denied" error

Make sure the AppImage is executable:
```bash
chmod +x Seria-*.AppImage
```

### Linux: Missing dependencies

If the AppImage won't run, you may need to install FUSE:
```bash
# Ubuntu/Debian
sudo apt install fuse libfuse2

# Fedora
sudo dnf install fuse fuse-libs
```

### Windows: SmartScreen warning

Windows may warn about unrecognized apps. This is normal for unsigned applications. Click "More info" → "Run anyway".

### macOS: "App cannot be opened" error

Right-click the app and select "Open" to bypass Gatekeeper.

### More Help

- [Troubleshooting Guide](../04%20-%20FAQ%20and%20Support/04.01%20-%20Current%20Limitations.md) - Common issues and solutions
- [FAQ](../04%20-%20FAQ%20and%20Support/04.00%20-%20FAQ.md) - Frequently asked questions
- [GitLab Issues](https://gitlab.com/parkerhdavis/Seria/-/issues) - Report a bug

---

**Ready to edit your first CSV?** Continue to [Your First CSV](../01%20-%20Cell%20View/01.01%20-%20Loading%20and%20Saving.md).
