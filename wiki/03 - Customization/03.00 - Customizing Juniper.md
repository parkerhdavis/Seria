TODO: Provide an overview of the types of customization available, including "forked" edits of existing recipes, creating custom recipes, importing/exporting configs*, and more

Note: We currently only have file-specific config stores, which save the app state for a file for the next time it's loaded. We're soon going to be implementing a global config store that can be used for things like column validation formulas (not implemented yet, but will be around our first release), column generation formulas (not implemented yet, but will be around our first release) custom themes (not implemented yet, but will be eventually). There will be an interface where you can move file-specific configs from the given file into the global store, at which point it will be available to all files.